server:
  port: ${PORT:8080}

eureka:
  instance:
    hostname: easycloud-registry-service.herokuapp.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: https://${eureka.instance.hostname}/eureka/